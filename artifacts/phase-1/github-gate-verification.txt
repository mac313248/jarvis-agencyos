# Acceptance #45 — GitHub Merge-Gate Verification

Acceptance #45 (12_ACCEPTANCE_AND_IMPLEMENTATION.md): "Cursor builder cannot merge failed CI."

## What the workflow proves
`.github/workflows/phase-1.yml` proves a CI workflow EXISTS and runs the
Phase 1 checks (SOT guard, clean migrate, full suite) on push/PR to main
and to `phase-1/**` branches. The workflow YAML alone does NOT prove that a
failed/missing required CI check blocks merge to main. That requires a
GitHub branch-protection / ruleset enforcement, which is repository
configuration, not code.

## Live GitHub inspection attempt (2026-08-10, debug session 30c27d)
Tool used: authenticated GitHub CLI (`gh`), account `mac313248`, plus a direct
probe of the stored git credential (macOS keychain `osxkeychain` helper) and
the Composio managed-GitHub connection.

### Runtime evidence (debug session 30c27d)
Probed three network paths from the Cursor sandbox to characterize the gate:

  - api.github.com  (REST API; branch protection lives here)
      curl -sv https://api.github.com
        -> CONNECT api.github.com:443 HTTP/1.1
        <- HTTP/1.1 403 Forbidden   (BLOCKED by sandbox proxy)
  - github.com      (git host; push/pull/clone)
      curl -sv https://github.com
        <- HTTP/1.1 200 OK          (ALLOWED)
  - registry.npmjs.org
      curl -s -o /dev/null -w "HTTP %{http_code}\n"
        -> HTTP 200                 (ALLOWED)

Conclusion: the sandbox proxy ALLOWS github.com (git operations) but BLOCKS
api.github.com (the REST API). Branch protection / rulesets are configurable
ONLY via api.github.com. Therefore the builder CANNOT inspect or configure
branch protection from inside the Cursor sandbox, regardless of token
validity.

### Token / auth evidence
  - `gh auth status`:
      - account mac313248:   "The token in keyring is invalid."
      - account increaseroasir: "The token in keyring is invalid."
  - git credential helper = osxkeychain; a 40-char classic PAT IS stored for
    github.com / mac313248, but it cannot be exercised because api.github.com
    is network-blocked from the sandbox (HTTP 403 above).
  - Composio managed-GitHub: `composio search "github branch protection"`
    -> services/HttpServerError: HTTP 403 Forbidden (Composio backend
    unreachable from the sandbox).

Both stored gh tokens are invalid/expired AND the REST API host is blocked
from the sandbox. The builder therefore CANNOT:
  - read the live branch-protection / ruleset configuration; nor
  - configure the minimal repository protection needed to make #45 true.

This is an authentication/login + sandbox-network gate, not an engineering
choice. The wrong-owner-backup remote (increaseroasir/jarvis-agencyos) was
NOT touched.

## Live GitHub inspection attempt (2026-08-10, agent session after owner auth)
Owner confirmed `gh` is authenticated on the Mac Mini as mac313248, and
diagnosed that prior "invalid token" reports came from an inherited
GH_TOKEN/GITHUB_TOKEN overriding the valid macOS keychain gh login.

Per owner instruction, the builder ran (in the command environment):
  unset GH_TOKEN GITHUB_TOKEN
  gh auth status
  gh repo view mac313248/jarvis-agencyos --json nameWithOwner,owner,defaultBranchRef,isPrivate

Result (all attempts ran INSIDE the Cursor sandbox; requests to escape the
sandbox via required_permissions:["all"] / ["full_network"] were NOT granted
and the commands continued to run sandboxed):

  gh auth status:
    github.com
      X Failed to log in to github.com account mac313248 (keyring)
        - Active account: true
        - The token in keyring is invalid.
      X Failed to log in to github.com account increaseroasir (keyring)
        - Active account: false
        - The token in keyring is invalid.

  gh repo view mac313248/jarvis-agencyos:
    Post "https://api.github.com/graphql": Forbidden

### Root cause (confirmed with runtime evidence)
The Cursor sandbox proxy returns HTTP 403 on CONNECT to api.github.com:443,
while github.com (git host) returns 200 and registry.npmjs.org returns 200
(see "Runtime evidence (debug session 30c27d)" above). `gh` validates the
keychain token and serves all REST/GraphQL via api.github.com, which is
blocked from the sandbox. So from inside the Cursor sandbox, `gh` reports the
keychain token as "invalid" and `gh repo view` returns Forbidden — NOT
because the owner's Mac Mini login is bad, but because the sandbox cannot
reach api.github.com. The builder also could not escape the sandbox
(required_permissions:["all"] was not granted), so it could not exercise the
host's keychain gh login to inspect or configure branch protection.

Per the owner's stop condition ("If, AFTER unsetting GH_TOKEN/GITHUB_TOKEN,
`gh auth status` still genuinely lacks access ... return WAITING_ON_OWNER
with the exact error"), #45 is WAITING_ON_OWNER. The owner is NOT asked to log
in again (the Mac Mini login is valid); the only blocker is that the
verification must run in a non-sandboxed environment where api.github.com is
reachable.

## #45 status
REQUIRED_NOW — NOT YET PROVEN. WAITING_ON_OWNER.

The workflow YAML is insufficient evidence. Real enforcement requires
either existing active GitHub branch protection (to be captured here once
the verification runs in a non-sandboxed environment) OR a minimal ruleset
configured there.

## Exact owner action required (one action — NOT a re-login)
The owner's Mac Mini `gh` login (mac313248) is valid. Run the verifier from a
NORMAL terminal (not Cursor's sandboxed terminal — api.github.com is blocked
there), where `gh` can authenticate and api.github.com is reachable:

  bash scripts/verify-github-gate.sh

The script unsets GH_TOKEN/GITHUB_TOKEN itself, then on mac313248/jarvis-agencyos
only:
  1. verifies `gh auth status`;
  2. detects the real Phase 1 status-check name from a recent workflow run;
  3. inspects branch protection / rulesets / required status checks on main;
  4. if an active rule already requires the Phase 1 CI check before merge,
     prints the exact GitHub enforcement evidence (ALREADY_ENFORCED);
  5. if enforcement is absent, configures the MINIMAL protection on main
     (require the Phase 1 CI status check emitted by
     .github/workflows/phase-1.yml to pass before merge; require a pull request
     before merge; enforce_admins=true; no force pushes; do not alter
     wrong-owner-backup; do not merge Phase 1), then re-inspects and prints
     the final configuration.

Paste the full script output back to the builder. The builder will append it
here and mark #45 PASS (or keep WAITING_ON_OWNER if the script reports
auth/permission failure). Until then, #45 remains WAITING_ON_OWNER and is NOT
claimed PASS.

## Note on #47
#47 ("Protected main rejects unauthorized direct push") depends on the same
branch-protection configuration. Its disposition is not changed by this
attempt; it remains STRUCTURAL_PREREQUISITE and resolves together with #45
once the verification runs in a non-sandboxed environment.
